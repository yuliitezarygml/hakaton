# Задачи на улучшение проекта

## 🔴 Критично

- [ ] **Заменить regex-парсер HTML на нормальный**
  Использовать `golang.org/x/net/html` вместо regexp в `services/fetcher.go`.
  Текущий парсер ломается на вложенных тегах, JSX-атрибутах с `>`, CDATA и современных новостных сайтах.

- [ ] **Исправить работу расширения на SPA-сайтах**
  `content.js` не видит текст на React/Vue/Angular сайтах — DOM ещё пуст в момент инъекции.
  Решение: добавить MutationObserver или retry с задержкой.

- [ ] **Добавить кэш результатов на бэкенде**
  Один URL анализируется каждый раз заново. Нужен in-memory кэш на 30 минут.
  Убрал бы 80% повторных запросов к ИИ.

- [ ] **Улучшить логику верификации при `score ≤ 7`**
  Если `missing_evidence` и `verifiable_facts` пустые — верификация ищет предложения с цифрами и даёт плохие запросы в Serper.
  Нужна более умная стратегия извлечения утверждений.

---

## 🟡 Важно

- [ ] **История анализов**
  Закрыл попап/вкладку — результат потерян.
  Сохранять в `localStorage` (фронтенд) и `chrome.storage.local` (расширение).

- [ ] **Валидация входных данных на бэкенде**
  Сейчас можно послать 1 МБ текста или невалидный URL — бэкенд примет всё.
  Нужны: лимит длины текста, проверка формата URL, rate limiting на эндпоинты.

- [ ] **Ограничить CORS**
  Сейчас `Access-Control-Allow-Origin: *` — любой сайт может слать запросы к localhost:8080.
  Ограничить только нужными origin.

- [ ] **Прогресс-бар во время SSE-анализа в расширении**
  После фазы скана прогресс исчезает. Пользователь не знает сколько ждать.
  Добавить индикатор прогресса или спиннер с текстом шага.

- [ ] **Добавить иконки расширению**
  Сейчас серый пазл Chrome (выглядит как сломанное расширение).
  Сгенерировать иконки 16/48/128px (план уже есть в `extension/generate-icons.js`).

---

## 🟢 Функциональные улучшения

- [ ] **Режим сравнения двух текстов**
  Анализировать оригинал и пересказ/перевод рядом. Показать различия в оценке.

- [ ] **Экспорт результата**
  Сейчас только "скопировать JSON". Добавить экспорт в PDF и Markdown.
  Полезно для журналистов и исследователей.

- [ ] **Автоопределение языка**
  Промпт и результаты всегда на русском. Английский текст анализируется нормально, но ответ на русском — неудобно.
  Добавить определение языка входного текста и локализованный вывод.

- [ ] **Поле ввода URL в расширении**
  Сейчас можно анализировать только активную вкладку или выделенный текст.
  Добавить текстовое поле для ввода произвольного URL прямо в попапе.

---

## ⚙️ Инфраструктура

- [ ] **Инициализировать git-репозиторий**
  Весь проект без версионирования. Без git нет возможности откатить изменения.
  `git init && git add . && git commit -m "initial commit"`

- [ ] **Написать тесты**
  Нет ни одного теста. Приоритетные кандидаты:
  - `services/fetcher.go` — `extractText()`
  - `services/analyzer.go` — `extractJSON()`, `fixJSONTypes()`
  - `services/serper.go` — `extractKeywords()`

- [ ] **Добавить Docker**
  Запустить на другой машине = вручную ставить Go, Node, настраивать `.env`.
  Написать `Dockerfile` + `docker-compose.yml` для запуска одной командой.
