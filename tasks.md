# Tasks

## Выполнено

### Полное описание проектов и функционал
**ANALYST (Fact Guard)** — это централизованная экосистема для проверки достоверности информации, работающая на базе ИИ. Система автоматически обнаруживает дезинформацию, логические сбои и манипуляции.

**Ключевые возможности проекта:**
*   **Единое ядро на Go:** Масштабируемый бэкенд обрабатывает запросы с любых клиентов, использует очередь для балансировки нагрузки ИИ, обходит SPA-сайты и кэширует ответы в Redis на 24 часа. Данные хранятся в PostgreSQL. Распознает Rate Limits от провайдеров.
*   **AI-Анализ с кросс-проверкой:** ИИ (Groq/OpenRouter) оценивает достоверность, ищет фейки и выводит вердикт (0-10 баллов). Веб-поиск (Serper) обеспечивает проверку фактов через Google. Уникальная функция "Цепочки источников" анализирует искажения смысла от оригинала к копиям.
*   **Мультиплатформенность:**
    *   *Расширение для Chrome:* Оценка статей в один клик, локальная история, подсветка проблем.
    *   *Telegram Бот:* Принимает URL и пересланные сообщения, а также умеет анализировать видео с помощью Gemini.
    *   *Web Интерфейс:* Удобный SPA для работы с текстами и публичными ссылками на результаты (система шеринга).
*   **Панель управления (Admin):** Инструментарий мониторинга в реальном времени — живые логи (WebSocket), дашборды, прямое управление Docker-контейнерами и просмотр лимитов.
*   **Гибкая инфраструктура (Docker):** Компоненты разворачиваются одной командой (`docker-compose up`). Nginx маршрутизирует трафик и гарантирует бесперебойный стриминг (SSE).

---

### Бэкенд (Go)

- [x] **Фетчер SPA** — фолбеки для JS-сайтов: ld+json → og meta-теги (`services/fetcher.go`)
- [x] **Rate limit время** — читает `Retry-After` / `X-RateLimit-Reset-Requests` из 429, логирует сколько ждать (`services/groq.go`, `services/openrouter.go`)
- [x] **GET /api/limits** — эндпоинт с текущими rate limit данными по провайдерам (`services/ratelimit.go`, `handlers/analyzer.go`, `main.go`)

### Админ-панель

- [x] **Редизайн** — IBM Plex Mono + Bebas Neue, сетка фона, scanlines, live WS-логи, API-тестер (`admin/index.html`)

### Chrome Расширение

- [x] **userId + история** — UUID на пользователя, хранит 30 записей 7 дней, просмотр по кнопке 🕐 (`popup.js`)
- [x] **Авто-скан без анимации** — при открытии popup тихо сканирует страницу, анимация только при ручном нажатии (`popup.js`, `content.js`)
- [x] **Кеш** — та же страница → мгновенный результат из кеша, кнопка 🔄 для принудительного рескана (`popup.js`)
- [x] **Уведомление в углу** — floating div в правом нижнем углу страницы после анализа (`popup.js`)
- [x] **Facebook поддержка** — `content.js` использует `div[dir="auto"]` + `[role="article"]`, overlay крепится к `<html>`, manifest добавлен `facebook.com` в host_permissions

### Telegram Bot

- [x] **Новая папка** `telegram-bot/` — отдельный Go-модуль (`main.go`, `analyzer.go`, `formatter.go`)
- [x] **SSE-клиент** — стримит `/api/analyze/stream`, обновляет сообщение в процессе
- [x] **Форматирование** — балл + прогресс-бар + вердикт + манипуляции + логические ошибки
- [x] **/cancel** — остановить текущий анализ, поддержка нескольких пользователей
- [x] **Видеоанализ** — Gemini Files API: загружает видео → ждёт ACTIVE → `gemini-2.5-flash` анализирует (`telegram-bot/gemini.go`)

### Инфраструктура

- [x] **Один .env** — бот читает `../.env` из корня, удалён отдельный `.env.example` из `telegram-bot/`, создан общий `.env.example`
- [x] **Docker** — исправлен nginx (SSE буферизация, таймауты, server_name), добавлен сервис telegram-bot, env_file вместо ручного перечисления переменных, healthcheck на postgres и backend

---

## В очереди

- [x] Webhook режим для Telegram бота — автовыбор через `WEBHOOK_URL` в `.env`
- [x] Кнопки inline в боте (повторный анализ, поделиться)
- [ ] Показ rate limits в админ-панели (использовать `/api/limits`)
- [ ] Уведомление расширения если сайт уже в истории при переходе

---

## 🔗 Цепочка источников (реализовано 2026-02-28)

> Находит оригинал статьи и все производные пересказы, показывает где и как менялся смысл

### Как работает

1. Загружает исходную статью по URL
2. AI извлекает тему, поисковый запрос и ключевые утверждения
3. Serper API ищет похожие публикации в сети
4. Для каждой найденной статьи: загружает → AI сравнивает с оригиналом → извлекает искажения
5. Стримит результаты по SSE по мере анализа каждого узла

### Бэкенд

- [x] **`services/chain.go`** — `ChainService` + `ChainNode` + `Distortion` + `ChainResult`
  - `BuildChain(ctx, url, emit)` — основной метод, стримит `ChainEvent`
  - `extractTopicAndClaims(text)` — AI → тема + search_query + key_claims
  - `analyzeRelatedArticle(url, claims)` — AI сравнивает статью с оригиналом
  - Типы искажений: `exaggeration` / `omission` / `addition` / `change` + `distortion_score` 0–10
- [x] **`handlers/chain.go`** — `POST /api/chain/stream` SSE endpoint
  - События: `chain_start` → `chain_progress` → `chain_node` → `chain_done` / `chain_error`
- [x] **`main.go`** — зарегистрирован `/api/chain/stream`

### Chrome расширение

- [x] **`popup.html`** — кнопка «🔗 Цепочка источников» в idle + `view-chain` с карточками
- [x] **`popup.js`** — `startChain(url)`, `handleChainEvent()`, `renderChainNode()` — SSE + рендер
- [x] **`popup.css`** — стили: `.chain-node`, `.chain-dist-type.*`, `.chain-score.{low|med|high}`

---

## 🎥 Gemini Video Analysis (реализовано!)

> Нужен `GEMINI_API_KEY` из [aistudio.google.com](https://aistudio.google.com) (бесплатно)

- [x] **Telegram bot — обработка видео** — когда пользователь присылает/пересылает видео, бот его скачивает (`telegram-bot/main.go`)
- [x] **Gemini Files API** — загрузить видео в Gemini Files API (`telegram-bot/gemini.go`)
- [x] **Транскрипция + дезинформация** — Gemini одним запросом: транскрибирует речь + анализирует на манипуляции
- [x] **Добавлен** `GEMINI_API_KEY` в `.env` и `docker-compose.yml`

- [x] **????? ?????????????** � ?????????? \ound_evidence\ ?? ?????? (??????, ?????????)
- [x] **UI ??? ?????????????** � ??????????? ? ????, ??????????, Fact Guard, Admin Panel
- [x] **100% ????????** � ??????????? ??????? ??? ADEVARAT ?? 100% ?????????? ??????????
