# Tasks

## Выполнено

### Бэкенд (Go)

- [x] **Фетчер SPA** — фолбеки для JS-сайтов: ld+json → og meta-теги (`services/fetcher.go`)
- [x] **Rate limit время** — читает `Retry-After` / `X-RateLimit-Reset-Requests` из 429, логирует сколько ждать (`services/groq.go`, `services/openrouter.go`)
- [x] **GET /api/limits** — эндпоинт с текущими rate limit данными по провайдерам (`services/ratelimit.go`, `handlers/analyzer.go`, `main.go`)

### Админ-панель

- [x] **Редизайн** — IBM Plex Mono + Bebas Neue, сетка фона, scanlines, live WS-логи, API-тестер (`admin/index.html`)

### Chrome Расширение

- [x] **userId + история** — UUID на пользователя, хранит 30 записей 7 дней, просмотр по кнопке 🕐 (`popup.js`)
- [x] **Авто-скан без анимации** — при открытии popup тихо сканирует страницу, анимация только при ручном нажатии (`popup.js`, `content.js`)
- [x] **Кеш** — та же страница → мгновенный результат из кеша, кнопка 🔄 для принудительного рескана (`popup.js`)
- [x] **Уведомление в углу** — floating div в правом нижнем углу страницы после анализа (`popup.js`)

### Telegram Bot

- [x] **Новая папка** `telegram-bot/` — отдельный Go-модуль (`main.go`, `analyzer.go`, `formatter.go`)
- [x] **SSE-клиент** — стримит `/api/analyze/stream`, обновляет сообщение в процессе
- [x] **Форматирование** — балл + прогресс-бар + вердикт + манипуляции + логические ошибки
- [x] **/cancel** — остановить текущий анализ, поддержка нескольких пользователей

### Инфраструктура

- [x] **Один .env** — бот читает `../.env` из корня, удалён отдельный `.env.example` из `telegram-bot/`, создан общий `.env.example`

---

- [x] **Docker** — исправлен nginx (SSE буферизация, таймауты, server_name), добавлен сервис telegram-bot, env_file вместо ручного перечисления переменных, healthcheck на postgres и backend

## В очереди

- [x] Webhook режим для Telegram бота — автовыбор через `WEBHOOK_URL` в `.env`
- [x] Кнопки inline в боте (повторный анализ, поделиться)
- [ ] Показ rate limits в админ-панели (использовать `/api/limits`)
- [ ] Уведомление расширения если сайт уже в истории при переходе

---

## 🎥 Gemini Video Analysis (реализовать!)

> Нужен `GEMINI_API_KEY` из [aistudio.google.com](https://aistudio.google.com) (бесплатно)

- [ ] **Telegram bot — обработка видео** — когда пользователь присылает/пересылает видео, бот его скачивает (`telegram-bot/main.go`)
- [ ] **Gemini Files API** — загрузить видео в Gemini Files API (`telegram-bot/gemini.go`)
- [ ] **Deepfake-детекция** — Gemini 2.0 Flash анализирует кадры на признаки подделки лица/фона
- [ ] **Транскрипция + дезинформация** — Gemini одним запросом: транскрибирует речь + анализирует на манипуляции
- [ ] **Форматирование результата** — отдельный блок в боте: 🎭 Deepfake вердикт + 📝 анализ речи
- [ ] **Добавить** `GEMINI_API_KEY` в `.env` и `docker-compose.yml`
